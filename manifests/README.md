# Manifests

This folder contains lightweight repo metadata only.

The public repo intentionally does not commit the local junction inventory or copied-file manifests from the private working kit because those were machine-specific.
